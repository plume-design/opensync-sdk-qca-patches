--- a/frontends/nss/ecm_nss_ipv4.c
+++ b/frontends/nss/ecm_nss_ipv4.c
@@ -1075,6 +1075,14 @@ static unsigned int ecm_nss_ipv4_ip_proc
 	}
 
 	/*
+	 * Drop packets with TTL 0
+	 */
+	if (ip_hdr.ttl < 1) {
+		DEBUG_TRACE("Dropping TTL 0 packet\n");
+		return NF_DROP;
+	}
+
+	/*
 	 * Process only IPv4 packets
 	 */
 	if (!ip_hdr.is_v4) {
